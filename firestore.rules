rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow users to read and write to the database
      allow read, write: if request.auth != null;
    }
  }
}
